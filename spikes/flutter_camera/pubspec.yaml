name: flutter_camera_smoke
description: Prueba de humo de cámara para la evaluación de stack.
publish_to: 'none'

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  camera: ^0.11.2
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
